{
  "name": "theview-ai-functions",
  "description": "Secure server-side operations for CHEE CHAN STOCK",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "lint": "node --check index.js",
    "deploy": "firebase deploy --only functions:completeFirstPasswordChange"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  }
}
